import XCTest
@testable import SwiftUI_CarouselView

final class SwiftUI_CarouselViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUI_CarouselView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
